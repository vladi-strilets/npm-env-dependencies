{
	"name": "npm-env-dependencies",
	"version": "0.0.3",
	"description": "Module to specify dependencies for different enviroment variables for package.json.",
	"main": "index.js",
	"bin": {
		"npm-env": "index.js"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/vladi-strilets/npm-env-dependencies"
	},
	"keywords": [
		"dependency",
		"install",
		"dependencies",
		"multiples",
		"enviroment",
		"os",
		"modules",
		"package.json"
	],
	"author": "Vladimir Strilets",
	"license": "MIT",
	"dependencies": {
		"cross-spawn": "^4.0.0"
	},
	"devDependencies": {},
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}
